//Datatypes and ECMA standards

"use strict"; //all JS code in newer standard

//alert(3 + 3); no output bc we're using node environment not browser

let a = 23;             //number
let bigint = 1000000;   //bigNumber
let isLoggedin = true;  //boolean
let b = "User";         //string
let c = undefined;      //undefined
let d = null;           //object

console.table([typeof(a), typeof(bigint), typeof(isLoggedin), typeof(b), typeof(c), typeof(d)])


